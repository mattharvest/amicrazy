<div class="hero-unit">
<h1>So, what's YOUR crazy story?</h1>
</div>
<div class="container-fluid offset2">
<%if signed_in? %>
	<div class="row-fluid">
		<div class="span6">
			<%= render 'shared/post_form' %>
		</div>
		<div class="span6">

		</div>
	</div>
	<div class="row-fluid">
		<% unless current_user.posts.empty? %>
			<table class="table table-striped">
				<%= render current_user.posts %>
			</table>
		<% end %>
	</div>
<% else %>
	<h1>You might ask, "Am I Crazy?"</h1> <h6>(sign up, and you can find out)</h6>
	<%= link_to "Sign up now", signup_path %>
<% end %>
</div>